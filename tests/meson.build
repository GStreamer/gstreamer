# FIXME: make check work on windows
if host_machine.system() != 'windows'
  subdir('check')
endif
if not get_option('disable_examples')
  subdir('examples')
endif
#subdir('files')
# FIXME: subdir('icles')
