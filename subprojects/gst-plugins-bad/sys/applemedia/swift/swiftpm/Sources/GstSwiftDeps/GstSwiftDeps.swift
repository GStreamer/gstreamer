// This file exists only to make SwiftPM build all dependencies which we cannot yet build with Meson.
// SwiftPM will statically link the dependencies into a dummy library, which we will then link into the Meson build.
